# <%= name %>

> <%= description %>

## Development

To start developing in the project run:

```bash
npm run serve
```

Then head to `http://localhost:9001` in your browser.

## Tests

To run tests run:

```bash
npm run tests
```
